# Core
BOT_TOKEN=
FORWARD_FROM_CHAT_ID=
ADMIN_USER_ID=
ADMIN_USERNAME=
BASE_REQUIRED_CHANNEL=MurkoffProjectsleep
DB_PATH=data/bot.db
# Max media requests per user per hour (0 = disabled). Admins bypass.
RATE_LIMIT_PER_HOUR=30
# Local Bot API server URL (empty = api.telegram.org, 50MB cap).
# Point at a self-hosted telegram-bot-api to raise the upload limit.
TELEGRAM_LOCAL_API_URL=
# Max upload size in MB. 50 on cloud API; up to 2000 with a local API server.
TELEGRAM_UPLOAD_LIMIT_MB=50

# Redis
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_PENDING_TTL_MINUTES=15

# Instagram
INSTAGRAM_DOWNLOAD_TIMEOUT_SECONDS=600
INSTAGRAM_MAX_FILE_SIZE_MB=2048
INSTAGRAM_YTDLP_BIN=
INSTAGRAM_GALLERY_DL_BIN=
INSTAGRAM_PYTHON_BIN=
INSTAGRAM_COOKIES_FROM_BROWSER=
INSTAGRAM_COOKIES_FILE=
INSTAGRAM_USERNAME=
INSTAGRAM_PASSWORD=
INSTAGRAM_SESSION_ID=
INSTAGRAM_INSTALOADER_SESSIONFILE=
INSTAGRAM_INSTAGRAPI_SETTINGS_FILE=
INSTAGRAM_FALLBACK_SCRIPT=

# Reddit
REDDIT_PYTHON_BIN=
REDDIT_FALLBACK_SCRIPT=
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=
REDDIT_DOWNLOAD_TIMEOUT_SECONDS=
REDDIT_FEED_ENABLED=false
REDDIT_FEED_SUBREDDIT=
REDDIT_FEED_CHAT_ID=
REDDIT_FEED_INTERVAL_HOURS=24
REDDIT_FEED_FETCH_LIMIT=25
REDDIT_FEED_POSTS_PER_TOPIC=3
REDDIT_FEED_TOP_WINDOW=day
REDDIT_FEED_POLL_MINUTES=15

# Video tools
FFMPEG_BIN=
# Default MP4 quality shown first in the picker: best | 1080 | 720 | 480
DEFAULT_VIDEO_QUALITY=best
# Reuse Telegram file_id for repeat downloads (skip re-download). true/false.
MEDIA_CACHE_ENABLED=true
# Default UI language for new users: en or ru
DEFAULT_LANG=en
# Deep-linked group posts: seconds quoted in the "save it" notice, and how long
# until the copied post is actually deleted. Keep them equal so the notice is
# truthful; they are separate only because the two were specified apart.
FORWARD_NOTICE_SECONDS=30
FORWARD_DELETE_SECONDS=60

# GIF/animation shown with the /start welcome message (empty = text only)
WELCOME_GIF_PATH=logo/stash-electric.gif

# Caption put on every file the bot copies out of the source group.
# Leave empty to use the caption of the source post instead.
DELIVERY_CAPTION=استیکر توت فرنگی
